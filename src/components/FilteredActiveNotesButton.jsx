import React from "react";

function FilteredActiveNotesButton({ onFilteredActive }) {
  return <button onClick={onFilteredActive}>Catatan Aktif</button>;
}

export default FilteredActiveNotesButton;
